= link_to "Home", root_path, :class => 'brand'
%ul.nav
  - navigation.each do |nav_item|
    %li{:class => ( (nav_item.tr('/','') == @requested_action) ? 'active' : nil) }
      %a{:href => nav_item}=title_from_url nav_item
